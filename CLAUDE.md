# 교육 네비게이터 프로젝트 지침

이 프로젝트에서 작업할 때는 [`지침.txt`](./지침.txt)의 40개 규칙을 최우선 구현 기준으로 삼는다.
아래는 매 세션 시작 시 반드시 상기해야 할 핵심 요약이다. 전문은 항상 `지침.txt`를 확인하고,
이 요약과 충돌하면 `지침.txt`의 명시적 문구를 우선한다. 상세 규칙 분류는 [`AGENTS.md`](./AGENTS.md)에도 정리되어 있다.

## 반드시 지켜야 할 핵심 원칙

- **데이터**: 업로드된 데이터에 실제로 존재하는 값만 사용. 원본 파일은 읽기 전용, 수정 금지.
  ID 기반 연결만 사용 (`employee_id → job_id → level → jqc_id → course_id → schedule_id`).
  데이터에 없는 직원/직무/JQC/과정/일정/인증조건을 임의로 만들지 않는다.
- **레벨**: `current_level`을 그대로 사용 (재계산 금지). 목표는 LV4→LV3→LV2→LV1, LV1 다음은
  '특급 엔지니어'(명칭만 표시, JQC/교육/진행률 생성 금지).
- **교육이력**: '수료'/'우수' = 이수 완료, '재교육' = 미완료. 사용자가 추가/해제 가능,
  '초기 이력으로 되돌리기'로 원복. 변경사항은 `employee_id`별 `localStorage`에 저장.
- **JQC/준비도**: 현재 job_id + 다음 목표 레벨에 연결된 JQC만 조회 (LV1은 조회 안 함).
  모든 연결은 REQUIRED로 취급. 준비도 = 이수한 고유 과정 수 ÷ 전체 고유 과정 수 × 100.
  연결 교육 없으면 '교육정보 없음'. 0%='학습 시작 전', 1~99%='학습 진행 중', 100%='JQC 평가 준비 완료'
  (100%를 취득/인증 완료로 표현 금지).
- **추천/일정**: 준비도 100% 미만 JQC = '준비가 필요한 JQC'. 추천 교육은 미이수 + 연결된 과정만,
  중복 과정은 1회만 표시하고 연계 JQC 모두 표시. 일정은 오늘 이후만 신청 가능, 가까운 순 정렬,
  일정 없어도 추천에서 제외하지 않음.
- **모의 신청**: 실제 접수 아님. `employee_id`+`schedule_id` 기준 `localStorage` 저장,
  동일 신청 중복 방지, 본인 사번 신청만 표시.
- **개발 방식**: React 기반 반응형 웹앱. 외부 API/DB/실로그인/SSO/Gemini API/별도 API 키 금지.
  모든 계산은 브라우저 내 결정적 처리(같은 데이터 → 같은 결과). 기존 정상 기능 유지하며 확장.
  오류는 최소 변경으로 근본 원인 수정. 구현 후 데이터 연결/계산 테스트, 결과는 한국어로 설명.
  데이터나 규칙이 불충분하면 임의 결정하지 말고 무엇이 부족한지 명확히 알린다.

## 작업 시 체크리스트

1. 기능을 만들거나 수정하기 전, 관련 규칙을 `지침.txt`에서 다시 확인한다.
2. 화면에 표시하거나 계산하는 값이 실제 업로드 데이터에서 나온 것인지 확인한다.
3. 구현 후 실제 데이터로 계산 결과를 검증하고, 변경 내용과 확인 방법을 한국어로 보고한다.
