# 교육 네비게이터 프로젝트 지침

이 프로젝트에서는 `지침.txt`의 규칙을 최우선 구현 기준으로 삼는다. 기능 추가, 수정, 검증, 설명 시 아래 지침을 지속적으로 준수한다.

## 역할과 데이터 원칙

- 데이터 기반 교육 서비스를 만드는 웹 개발자로서, 사용자가 제공한 교육 네비게이터 데이터에 근거해 React 기반 반응형 웹앱을 개발한다.
- 화면과 계산에는 업로드된 데이터에 실제로 존재하는 값만 사용한다.
- 원본 데이터는 읽기 전용이며 수정하지 않는다.
- 데이터 연결은 이름이 아닌 ID로 수행하며, 관계는 `employee_id → job_id → level → jqc_id → course_id → schedule_id` 순서로 추적한다.
- 데이터에 없는 직원, 직무, JQC, 교육과정, 일정, 인증 조건을 만들지 않는다.
- 외부 API, 데이터베이스, 실제 로그인, SSO, Gemini API, 별도 API 키를 추가하지 않는다.
- 브라우저 안에서 결정적으로 데이터를 처리·추천 계산한다. 동일 데이터에는 동일 결과를 보여야 한다.
- 데이터 또는 규칙이 부족할 경우 임의로 결정하지 말고 부족한 항목을 명확히 알린다.

## 직원과 레벨

- 사용자는 `employee_id`(사번)로 자신의 정보를 조회한다.
- 현재 레벨은 직원 데이터의 `current_level` 값을 그대로 사용하며, 교육이력·준비도로 재계산하거나 변경하지 않는다.
- 다음 목표 레벨은 `LV4 → LV3`, `LV3 → LV2`, `LV2 → LV1` 순서로 제안한다.
- LV1의 다음 목표는 `특급 엔지니어`로 표시한다.
- 특급 엔지니어에는 명칭만 표시하며 JQC, 교육, 프로그램, 진행률을 생성하지 않는다.

## 교육이력

- 교육결과가 `수료` 또는 `우수`이면 이수 완료로 처리하고, `재교육`이면 완료로 처리하지 않는다.
- 기존 이수 교육을 자동 선택해 표시하되 사용자가 추가·해제할 수 있게 한다.
- `초기 이력으로 되돌리기`는 원본 이수 상태를 복원한다.
- 사용자가 바꾼 이수 상태는 `employee_id`별 `localStorage`에 저장한다.

## JQC와 교육 준비도

- 현재 `job_id`와 다음 목표 레벨에 모두 연결된 JQC만 조회한다. LV1 사용자는 목표 JQC를 조회하지 않는다.
- JQC-교육 연결은 모두 `REQUIRED`로 취급하며 선택교육·대체교육 규칙을 추가하지 않는다.
- 준비도는 `이수한 연결 교육의 고유 과정 수 ÷ 전체 연결 교육의 고유 과정 수 × 100`으로 계산한다.
- 연결 교육이 없으면 0%가 아닌 `교육정보 없음`으로 표시한다.
- 0%는 `학습 시작 전`, 1~99%는 `학습 진행 중`, 100%는 `JQC 평가 준비 완료`로 표시한다.
- 준비도 100%를 JQC 취득 완료나 공식 인증 완료로 표현하지 않는다.

## 추천, 일정, 모의 신청

- 준비도 100% 미만인 목표 JQC를 `준비가 필요한 JQC`로 표시한다.
- 추천 교육은 준비가 필요한 JQC에 연결되고 현재 이수 상태가 아닌 과정이다.
- 동일 과정이 여러 JQC와 연결되면 한 번만 표시하고 관련 JQC를 모두 표시한다.
- 추천 이유에는 데이터 근거인 다음 목표 레벨과 연계 JQC를 표시한다.
- 일정은 `course_id`로 교육에 연결하고, 오늘 이후 시작하는 일정만 신청 가능하게 한다. 가까운 일정부터 정렬한다.
- 일정이 없어도 교육 추천 목록에서는 제거하지 않는다.
- 신청은 실습용 모의 신청이며 `employee_id`와 `schedule_id`를 기준으로 `localStorage`에 저장한다.
- 동일 직원의 동일 일정 중복 신청을 막고, 나의 신청 교육에는 현재 사번의 신청만 표시한다.

## 변경과 검증

- 정상 작동하는 기존 기능은 유지하며, 오류는 원인을 찾아 최소 변경으로 수정한다.
- 구현 후 데이터 연결과 계산을 테스트한다.
- 완료 보고는 한국어로 작성하며 변경 내용과 확인 방법을 포함한다.

## 원본 지침

- 세부 원문은 [`지침.txt`](./지침.txt)를 참조한다. 이 파일과 충돌하면 `지침.txt`의 명시적 규칙을 우선한다.
